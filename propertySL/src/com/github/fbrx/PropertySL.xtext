grammar com.github.fbrx.PropertySL 
	with org.eclipse.xtext.common.Terminals

generate propertySL "http://www.github.com/fbrx/propertySL"

Model:
	packages += Package*;

Package:
	'package' ID '{'
		DefaultLocale?
	'}'
;

DefaultLocale:
	'DEFAULT_LOCALE' '=' LOCALE
;

LOCALE:
	'de' | 'en'
;

